{
  "partitions": [{
    "defaults": {
      "hostname": "{service}:{port}/{endpoint}{dnsSuffix}"
    },
    "dnsSuffix" : "",
    "partition" : "aws",
    "partitionName" : "AWS Standard",
    "protocols" : ["http"],
    "services" : {
      "minio" : {
        "port": "9000",
        "endpoints": {}
      },
      "forecast": {
        "defaults" : {
          "hostname" : "mlapi:{port}/{service}{dnsSuffix}"
        },
        "port": "80",
        "endpoints": {}
      }
    }
  }]
}
