{
  "partitions": [{
    "defaults": {
      "hostname": "{service}:{port}/{endpoint}{dnsSuffix}"
    },
    "dnsSuffix" : "",
    "partition" : "aws",
    "partitionName" : "AWS Standard",
    "protocols" : ["http"],
    "services" : {
      "train": {
        "defaults": {
          "hostname": "api:{port}/{service}/{endpoint}{dnsSuffix}"
        },
        "port": "80",
        "endpoints": {}
      },
      "writer" : {
        "defaults": {
          "hostname": "minio:{port}"
        },
        "port": "9000",
        "endpoints": {}
      },
      "predict": {
        "defaults" : {
          "hostname" : "api:{port}/{service}/{endpoint}{dnsSuffix}"
        },
        "port": "80",
        "endpoints": {}
      },
      "login": {
        "defaults": {
          "hostname": "api:{port}/{service}{dnsSuffix}"
        },
        "port": "80",
        "endpoints": {}
      },
      "authenticator": {
        "defaults": {
          "hostname" : "api:{port}/users/me"
        },
        "port": 80,
        "endpoints": {}
      }
    }
  }]
}
