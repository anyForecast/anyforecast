# Python version required to run the project.
python: "3.10"
# Dependencies required to build packages. This field is optional.
build_dependencies:
  - pip
  - setuptools
  - wheel==0.37.1
# Dependencies required to run the project.
dependencies:
  - numpy
  - pandas
  - scikit-learn>1.0.0
  - click
  - skorch-forecasting @ git+https://github.com/skorch-forecasting-dev/skorch-forecasting.git
  - mlflow
  

